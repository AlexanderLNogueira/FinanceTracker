{
  "name": "finance-tracker",
  "version": "1.0.0",
  "description": "Simple web-based financial tracker",
  "type": "module",
  "scripts": {
    "start": "serve .",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
