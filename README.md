<!-- Finance Tracker
     A local-first, browser-only personal finance tracker.

     Running locally
       npm install
       npm start        # serves the project (http://localhost:3000 or similar)
       npm test         # runs the unit test suite

     Tech stack
       - Vanilla JavaScript (ES modules)
       - Chart.js v4.5.1 (MIT) — vendored locally at js/vendor/chart.js
       - localStorage persistence (no backend)
       - Node's built-in test runner for unit tests
-->